name: sewabustravel
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  # <<< TAMBAHAN UNTUK SESSION PERSISTEN >>>
  shared_preferences: ^2.2.3

  # --- CORE & STATE MANAGEMENT ---
  intl: ^0.20.2
  http: ^1.2.1 
  crypto: ^3.0.3
  
  # --- SQLITE & LOCAL STORAGE ---
  sqflite: ^2.3.4
  path_provider: ^2.1.3
  
  # --- SUPABASE & STORAGE ---
  supabase: ^2.10.0
  supabase_flutter: ^2.10.3
  
  # --- LOGISTIK & GEO (PERBAIKAN & PENAMBAHAN) ---
  timezone: ^0.9.4
  flutter_map: ^6.1.0        
  latlong2: ^0.9.1          
  geocoding: ^4.0.0         
  
  # WAJIB UNTUK LOKASI SAAT INI (LBS)
  geolocator: ^12.0.0       
  permission_handler: ^11.3.1 
  
  # --- MEDIA & FILE HANDLING ---
  image_picker: ^1.1.2      
  image_picker_for_web: ^3.0.0 
  path: ^1.9.0              

  # --- NOTIFICATIONS ---
  flutter_local_notifications: ^17.1.2 

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    # Tambahkan di sini jika kamu punya file gambar atau ikon custom
    # contoh:
    # - assets/images/